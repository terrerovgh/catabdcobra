---
import AppLayout from '../../layouts/AppLayout.astro';
import SwipeDeck from '../SwipeDeck.astro';
import ArtistCard from '../ArtistCard.astro';
import { artists } from '../../data/artists';
import { useDict, type Locale } from '../../i18n';

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
const t = useDict(locale);
---

<AppLayout locale={locale} title={t.artists.title}>
  <section class="pt-6">
    <div class="px-6">
      <h1 class="font-display text-3xl font-semibold">{t.artists.title}</h1>
      <p class="mt-1 text-sm text-ink-soft">{t.artists.sub}</p>
    </div>
    <SwipeDeck class="mt-2">
      {artists.map((artist) => <ArtistCard id={artist.id} artist={artist} locale={locale} />)}
    </SwipeDeck>
  </section>
</AppLayout>
